#
# scenario for all_one
#
roles:
  all_in_one:
    classes:
      - coe::base
    class_groups:
      - controller
      - compute
      - network_controller
  compute:
    classes:
      - coe::base
    class_groups:
      - compute
