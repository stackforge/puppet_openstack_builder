classes:
  - nova
  - nova::api
  - nova::config
  - nova::scheduler
  - nova::objectstore
  - nova::cert
  - nova::consoleauth
  - nova::conductor
  - "nova::network::%{network_service}"
  - nova::vncproxy
