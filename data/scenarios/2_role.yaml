#
# the two node 
#
roles:
  controller:
    classes:
      - naginator::control_target
      - coi::profiles::openstack::base
      - openstack::client
      - openstack::auth_file
      - openstack::test_file
    class_groups:
      - controller
      - network_controller
  compute:
    classes:
      - coi::profiles::openstack::base
      - naginator::compute_target
      - cinder::setup_test_volume
    class_groups:
      - compute
