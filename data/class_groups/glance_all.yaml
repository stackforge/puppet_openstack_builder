classes:
  - glance
  - glance::api
  - glance::config
  - glance::registry
  - "glance::backend::%{glance_backend}"
  - glance::cache::pruner
  - glance::cache::cleaner
  - "glance::notify::%{rpc_type}"
