class_groups:
  - glance_all
  - keystone_all
  - cinder_controller
  - nova_controller
  - horizon
  # this does not go here!!!
  - network_controller
  - "%{db_type}_database"
classes:
  - "nova::%{rpc_type}"
  - "%{network_service}::plugins::%{network_plugin}"
  - "%{network_service}::server"
